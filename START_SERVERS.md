# 🚨 Backend Server ไม่ได้รัน!

## ปัญหา:
Frontend ไม่สามารถเชื่อมต่อกับ Backend API ได้ เพราะ Backend server ไม่ได้รันอยู่

## วิธีแก้:

### 1. เปิด Terminal ใหม่สำหรับ Backend:
```bash
cd backend
npm run dev
```

คุณควรเห็น output แบบนี้:
```
[server]: Server is running at http://localhost:3001
[payload]: Payload Admin URL: http://localhost:3001/admin
```

### 2. ตรวจสอบว่า Backend รันแล้ว:
เปิดเบราว์เซอร์ไปที่:
- http://localhost:3001/admin (ควรเห็นหน้า login)
- http://localhost:3001/api/services (ควรเห็น JSON response)

### 3. หลังจาก Backend รันแล้ว:
Frontend จะทำงานได้ปกติ ไปที่:
- http://localhost:3000 (หน้าแรก)

## สรุป:
คุณต้องรัน **2 servers พร้อมกัน**:

### Terminal 1 - Backend (Port 3001):
```bash
cd backend
npm run dev
```

### Terminal 2 - Frontend (Port 3000):
```bash
cd frontend
npm run dev
```

## ตรวจสอบว่าทั้ง 2 servers รันอยู่:
```bash
# ตรวจสอบ Backend
curl http://localhost:3001/api/services

# ตรวจสอบ Frontend
curl http://localhost:3000
```

## หมายเหตุ:
- Backend จะต้องรันก่อน Frontend จึงจะดึงข้อมูลได้
- ถ้า Backend หยุดทำงาน Frontend จะแสดง error "Network error: fetch failed"
- ตรวจสอบว่า MongoDB รันอยู่ด้วย (ถ้าใช้ local MongoDB)
